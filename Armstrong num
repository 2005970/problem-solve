#include<stdio.h>
int main()
{
	int n,num;
	scanf("%d",&n);
	num=n;
	int last;
	int sum=0;
	while(n > 0)
	{
		int last = n % 10;
		sum = sum + (last * last * last );
		n = n / 10;
	}
	if(num == sum)
	{
		printf("Armstrong");
	}
	else
	{
		printf("No");
	}
	return 0;
}
